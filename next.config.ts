import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  //double renders in dev and warns about deprecated APIs
  reactStrictMode: true,
};

export default nextConfig;
